# Future Enhancements:
1. WebSocket transport — Alternative to SSE for bidirectional real-time
2. Partial sync — Sync only selected collections (beyond just filtering)
3. Delta encoding — For large entities, send only changed fields
4. Prometheus metrics — Production observability integration